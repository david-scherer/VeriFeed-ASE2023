# Post recognition service

## Initializing the virtual env

- Run `init_venv.sh`
- Activate the environment if you're not yet in it via `source venv/bin/activate`

## Running the service

`python app.py`
